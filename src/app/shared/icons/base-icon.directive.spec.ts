import { BaseIconDirective } from './base-icon.directive';

describe('BaseIconDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseIconDirective();
    expect(directive).toBeTruthy();
  });
});
